pipeline {
    agent any
    stages {
        stage('Run') {
            steps {
                sh ' touch file'
            }
       stage('Build') {
            steps {
                sh 'touch file'
            }
        }
    }
}
